class Solution {
    public double myPow(double x, int n) {
        
        if(n<0){
            long power=n;
            return Math.pow((1/x),-power);
            
        }
        else{
            return Math.pow(x,n);
        }
        
    }
}
